# Required for Deploy to Cloudflare button - you'll be prompted for these values
# Get your Anthropic API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# Generate with: openssl rand -hex 32 (or leave blank to auto-generate during setup)
GATEWAY_TOKEN=
